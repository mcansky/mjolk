%div
  %h2
    Edit
    = @group.name

  = form_for(:group, :url => { :action => "create" }) do |f|
    %p
      = f.label :name
      = f.text_field :name

    %p
      = f.label :desc
      %br
      = f.text_area :desc, :cols => 40, :rows => 3

    %p
      = f.submit("Add group")
