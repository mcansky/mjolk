.grid_15
  %p.beware This is alpha software, if you want to use please do, and report any error throught https://github.com/mcansky/mjolk/issues WARNING : DATA MIGHT BE ERASED every now and then (until otherwise said). Use at your own risk !

  %h1 Last 20 bookmarks

  - if current_user
    - cache(:action => 'index', :action_suffix => 'last_20_posts') do
      %ul
      - @posts.each do |post|
        %li.bookmark
          = render "/posts/short_post", :post => post
  - else
    - cache(:action => 'index', :action_suffix => 'public_last_20_posts') do
      %ul
      - @posts.each do |post|
        %li.bookmark
          = render "/posts/short_post", :post => post