<h2>Sign up</h2>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <p>Please fill up this quick form, it will allow us to contact you more easily if need be (updates etc).<br />
    Since it's an alpha version, keep in mind the website and many things in it might change quickly.</p>
  <p><%= f.label :email %><br />
  <%= f.text_field :email %></p>

  <p><%= f.label :name %><br />
  <%= session["devise.twitter_data"].inspect %>

  </p>
  
  <p>We also need you to fill this local password to allow you access even if Twitter is offline, or to secure some local process sometimes.</p>

  <p><%= f.label :password %><br />
  <%= f.password_field :password %></p>

  <p><%= f.label :password_confirmation %><br />
  <%= f.password_field :password_confirmation %></p>

  <p><%= f.submit "Sign up" %></p>
<% end %>

<%= render :partial => "/devise/shared/links" %>
